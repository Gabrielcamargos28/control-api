spring.application.name=ControlApi

# Database Configuration (PostgreSQL)
# Note: Replace with actual credentials if needed
spring.datasource.url=jdbc:postgresql://localhost:5432/controlapi
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Fallback to H2 if PostgreSQL is not available (for testing purposes)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
